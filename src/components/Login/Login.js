import React from 'react';
import styles from './Login.module.scss'

const Login = props => {
    return (
        <div>
            <h1>Login</h1>
            <form>

            </form>
        </div>
    );
};

export default Login;